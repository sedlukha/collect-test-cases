import { defineTotalsSuite } from "../../../helper"

// The tests live inside a shared helper. Their titles only exist while the
// code runs, so a text parser finds NOTHING here — runtime discovery does.
defineTotalsSuite("Cart totals")
